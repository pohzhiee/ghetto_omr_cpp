add_subdirectory(image)
add_subdirectory(tools)
add_subdirectory(dialog)
add_subdirectory(layer)


set(SOURCE
        ${SOURCE}
        PARENT_SCOPE
        )



set(HEADERS
        ${HEADERS}
        PARENT_SCOPE
        )